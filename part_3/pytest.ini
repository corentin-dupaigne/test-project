[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --html=reports/report.html
    --self-contained-html
markers =
    booking_full: marks tests requiring a full calendar drag-and-drop booking flow
